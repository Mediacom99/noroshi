---
phase: 03-ci-pipeline
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/storage/store.go
  - cmd/monitor/main.go
  - .golangci.yml
autonomous: true
requirements:
  - CICD-02
must_haves:
  truths:
    - "golangci-lint v2 runs locally with 5 linters and reports zero findings"
    - "All existing tests still pass after errorlint fixes"
    - "Build succeeds with CGO_ENABLED=0 after code changes"
  artifacts:
    - path: ".golangci.yml"
      provides: "golangci-lint v2 configuration with exactly 5 linters"
      contains: 'version: "2"'
    - path: "internal/storage/store.go"
      provides: "errorlint-clean sentinel comparisons"
      contains: "errors.Is(err, sql.ErrNoRows)"
    - path: "cmd/monitor/main.go"
      provides: "errorlint-clean server shutdown check"
      contains: "!errors.Is(err, http.ErrServerClosed)"
  key_links:
    - from: ".golangci.yml"
      to: "golangci-lint run"
      via: "linter reads config at repo root"
      pattern: "linters.default: none"
---

<objective>
Fix all existing lint violations and create the golangci-lint v2 configuration so the codebase passes clean.

Purpose: Per D-04, all existing lint warnings must be fixed -- no baseline exclusions, no nolint for pre-existing code. This plan makes the code lint-clean before the CI workflow is added.
Output: `.golangci.yml` with v2 config, errorlint-clean Go code, verified locally.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-ci-pipeline/03-CONTEXT.md
@.planning/phases/03-ci-pipeline/03-RESEARCH.md

<interfaces>
<!-- errorlint violations to fix — exact locations confirmed by grep -->

From internal/storage/store.go (3 instances):
```go
// Line 86: if err == sql.ErrNoRows {
// Line 105: if err == sql.ErrNoRows {
// Line 124: if err == sql.ErrNoRows {
```

From cmd/monitor/main.go (1 instance):
```go
// Line 124: if err := srv.ListenAndServe(); err != nil && err != http.ErrServerClosed {
```

<!-- b.rootCtx usage in internal/bot/ (potential contextcheck findings) -->
<!-- 20+ usages across handlers.go and callbacks.go — may need targeted //nolint -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix errorlint violations and create golangci-lint v2 config</name>
  <files>internal/storage/store.go, cmd/monitor/main.go, .golangci.yml</files>
  <read_first>
    - internal/storage/store.go (current sentinel comparisons at lines 86, 105, 124)
    - cmd/monitor/main.go (http.ErrServerClosed comparison at line 124)
    - .planning/phases/03-ci-pipeline/03-RESEARCH.md (golangci-lint v2 config pattern, errorlint fix pattern)
  </read_first>
  <action>
**Create `.golangci.yml` at repo root** (per D-03) with golangci-lint v2 configuration:

```yaml
version: "2"

linters:
  default: none
  enable:
    - bodyclose
    - contextcheck
    - errorlint
    - sloglint
    - sqlclosecheck

run:
  timeout: 5m
```

The `version: "2"` field is REQUIRED — without it, golangci-lint v2 misinterprets the config as v1 format.
`linters.default: none` disables all linters except the 5 explicitly enabled (per D-03 — no extras).

**Fix 3 errorlint violations in `internal/storage/store.go`:**

Add `"errors"` to the import block.

Line 86: Change `if err == sql.ErrNoRows {` to `if errors.Is(err, sql.ErrNoRows) {`
Line 105: Change `if err == sql.ErrNoRows {` to `if errors.Is(err, sql.ErrNoRows) {`
Line 124: Change `if err == sql.ErrNoRows {` to `if errors.Is(err, sql.ErrNoRows) {`

**Fix 1 errorlint violation in `cmd/monitor/main.go`:**

Add `"errors"` to the import block.

Line 124: Change `err != http.ErrServerClosed` to `!errors.Is(err, http.ErrServerClosed)`

The full line becomes:
```go
if err := srv.ListenAndServe(); err != nil && !errors.Is(err, http.ErrServerClosed) {
```

Note: `err != nil` is NOT an errorlint violation (comparing to nil is fine). Only comparisons against sentinel error values are flagged.

**These are the ONLY 4 errorlint findings in the codebase.** Test files use `err == nil` / `err != nil` which errorlint does not flag.
  </action>
  <verify>
    <automated>cd /Users/edoardo/Projects/noroshi && CGO_ENABLED=0 go build ./cmd/monitor/ && go vet ./... && go test ./...</automated>
  </verify>
  <acceptance_criteria>
    - `.golangci.yml` exists at repo root
    - `.golangci.yml` contains `version: "2"` as first non-comment line
    - `.golangci.yml` contains `default: none` under `linters:`
    - `.golangci.yml` contains exactly 5 linters: bodyclose, contextcheck, errorlint, sloglint, sqlclosecheck
    - `internal/storage/store.go` contains `"errors"` in import block
    - `internal/storage/store.go` contains `errors.Is(err, sql.ErrNoRows)` (3 occurrences)
    - `internal/storage/store.go` does NOT contain `err == sql.ErrNoRows`
    - `cmd/monitor/main.go` contains `"errors"` in import block
    - `cmd/monitor/main.go` contains `!errors.Is(err, http.ErrServerClosed)`
    - `cmd/monitor/main.go` does NOT contain `err != http.ErrServerClosed` (the sentinel comparison)
    - `CGO_ENABLED=0 go build ./cmd/monitor/` exits 0
    - `go vet ./...` exits 0
    - `go test ./...` exits 0
  </acceptance_criteria>
  <done>All 4 errorlint violations fixed. golangci-lint v2 config created with exactly 5 linters. Build, vet, and tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Verify golangci-lint passes locally and fix any remaining findings</name>
  <files>internal/storage/store.go, cmd/monitor/main.go, internal/bot/handlers.go, internal/bot/callbacks.go</files>
  <read_first>
    - .golangci.yml (config just created in Task 1)
    - internal/bot/handlers.go (b.rootCtx usage — potential contextcheck findings)
    - internal/bot/callbacks.go (b.rootCtx usage — potential contextcheck findings)
    - .planning/phases/03-ci-pipeline/03-RESEARCH.md (Pitfall 4: contextcheck false positives)
  </read_first>
  <action>
**Run golangci-lint locally to verify zero findings:**

```bash
golangci-lint run ./...
```

If golangci-lint is not installed locally, install it:
```bash
go install github.com/golangci/golangci-lint/v2/cmd/golangci-lint@v2.11.4
```

**Expected outcome:** Zero findings. The errorlint fixes from Task 1 resolve the known violations.

**If contextcheck flags `b.rootCtx` usage in bot handlers/callbacks:**

The bot intentionally stores the root context as a struct field (`b.rootCtx`) per project design — it is the application-scoped context from `signal.NotifyContext`, NOT the per-request Telegram handler context. This is a deliberate architectural decision documented in CLAUDE.md ("Context propagation: the root context from signal.NotifyContext flows everywhere").

Add targeted `//nolint:contextcheck // uses root context by design, not per-request context` comments ONLY on the specific methods that contextcheck flags. Per D-04: no blanket exclusions, but per-instance targeted nolint for intentional architectural patterns is acceptable.

The likely methods are in `internal/bot/handlers.go` (handleAdd, handleDelete, handleList, handleInterval, resolveEndpoint) and `internal/bot/callbacks.go` (handleDetail, handleRefresh, handleBack, handleConfirmDelete, handleQuickInterval, handleIntervalList).

Add the nolint comment on the function signature line, not on individual `b.rootCtx` calls. Example:
```go
func (b *Bot) handleAdd(c tele.Context) error { //nolint:contextcheck // uses root context by design, not per-request context
```

**If sloglint, bodyclose, or sqlclosecheck report findings:** Fix them following the same pattern — actual code fixes, not suppressions.

**After all findings are resolved, run the full verification suite again:**
```bash
golangci-lint run ./... && CGO_ENABLED=0 go build ./cmd/monitor/ && go vet ./... && go test ./...
```
  </action>
  <verify>
    <automated>cd /Users/edoardo/Projects/noroshi && golangci-lint run ./... && CGO_ENABLED=0 go build ./cmd/monitor/ && go vet ./... && go test ./...</automated>
  </verify>
  <acceptance_criteria>
    - `golangci-lint run ./...` exits 0 with zero findings
    - If contextcheck nolint comments were added, each one contains the explanation `// uses root context by design, not per-request context`
    - No blanket `exclude` or `skip-dirs` rules added to `.golangci.yml` (per D-04)
    - `CGO_ENABLED=0 go build ./cmd/monitor/` exits 0
    - `go vet ./...` exits 0
    - `go test ./...` exits 0
  </acceptance_criteria>
  <done>golangci-lint v2 runs with 5 linters and reports zero findings against the entire codebase. All contextcheck findings (if any) resolved with targeted nolint comments. Build, vet, and tests pass.</done>
</task>

</tasks>

<verification>
- `golangci-lint run ./...` exits 0
- `CGO_ENABLED=0 go build ./cmd/monitor/` exits 0
- `go vet ./...` exits 0
- `go test ./...` exits 0
- `.golangci.yml` has `version: "2"`, `default: none`, exactly 5 linters
- No `err == sql.ErrNoRows` or `err != http.ErrServerClosed` patterns remain in Go source
</verification>

<success_criteria>
The entire codebase passes golangci-lint v2 with bodyclose, contextcheck, errorlint, sloglint, and sqlclosecheck enabled, with zero findings and zero baseline exclusions.
</success_criteria>

<output>
After completion, create `.planning/phases/03-ci-pipeline/03-01-SUMMARY.md`
</output>
